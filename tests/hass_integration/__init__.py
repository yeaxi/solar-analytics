"""Integration tests exercising Solar Analytics against a real Home Assistant.

Runs only when ``pytest-homeassistant-custom-component`` is installed; the
fast pure-function suite under ``tests/*.py`` remains independent and does
not require this dependency.
"""
